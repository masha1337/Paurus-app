// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyDIMm6Q736BcMarxlTL9bfByqVLO9FiinA",
    authDomain: "paurus-app.firebaseapp.com",
    databaseURL: "https://paurus-app.firebaseio.com",
    projectId: "paurus-app",
    storageBucket: "paurus-app.appspot.com",
    messagingSenderId: "347122478578",
    appId: "1:347122478578:web:db6742f82fd7eb0f1a8f6b",
    measurementId: "G-ZYFW9RBGC9"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
